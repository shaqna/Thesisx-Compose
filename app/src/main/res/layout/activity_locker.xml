<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.cupboard.CupBoardActivity">

    <LinearLayout
        android:id="@+id/layout_key_locker"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginTop="@dimen/range_64"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="UseCompoundDrawables">

        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/locker_image"
            android:contentDescription="@string/not_found_image"/>

        <TextView
            android:id="@+id/tvLockerDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/InterMedium"
            android:layout_marginTop="@dimen/range_12"
            android:textSize="@dimen/size_18"
            android:textColor="@color/black"
            android:layout_marginHorizontal="@dimen/range_64"
            android:gravity="center"
            android:text="@string/ambil_di_loker_1_yaa"/>

        <TextView
            android:id="@+id/tvLockerCode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/InterMedium"
            android:layout_marginTop="@dimen/range_12"
            android:textSize="@dimen/size_24"
            android:textColor="@color/black"
            android:layout_marginHorizontal="@dimen/range_64"
            android:gravity="center"
            tools:text="1234"/>

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progress_bar"
        style="@style/ProgressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/primaryColor"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>